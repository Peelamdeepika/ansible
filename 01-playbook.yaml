- name: nginx install
  hosts: web
  become: yes
  tasks:
  - name: installing nginx server
    ansible.builtin.package:
     name: nginx
     state: present
  - name: starting nginx
    ansible.builtin.service:
     name: nginx
     state: started
     enabled: yes

   

